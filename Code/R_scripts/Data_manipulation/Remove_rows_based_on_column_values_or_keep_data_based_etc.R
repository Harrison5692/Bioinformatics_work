###### Subsetting, return the table based on values of a column values ##############

# example: Give me a table back if values in one column are below the integer 20

data_table <- subset(data_table_original, column_name < 20)